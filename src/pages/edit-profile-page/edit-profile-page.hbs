<div class="profile__back-arrow">
    <img src="/icons/back-arrow.svg" alt="back-arrow">
</div>
{{#> Container }}
    <div class="profile">
        <div class="profile__wrapper">
            {{> Avatar class='profile__avatar' }}

            <div class="profile__info">
                {{> ProfileInfo key='Почта' value='pochta@yandex.ru' }}
                {{> ProfileInfo key='Логин' value='ivanivanov' }}
                {{> ProfileInfo key='Имя' value='Иван' }}
                {{> ProfileInfo key='Фамилия' value='Иванов' }}
                {{> ProfileInfo key='Имя в чате' value='Иван' }}
                {{> ProfileInfo key='Телефон' value='+7 (909) 967 30 30' }}
            </div>

            <div class="edit-page__btn">
                {{> Button text='Сохранить' }}
            </div>

        </div>
    </div>
{{/Container}}
